<!-- Ambulances Section -->
    <section id="ambulances" class="bg-light-gray">
        <div class="container">
            <div class="row">
                <div class="col-lg-12 text-center">
                    <h2 class="section-heading">Our Amazing Ambulances</h2>
                    <h3 class="section-subheading text-muted">For each project, the team rallied around one ambulance.</h3>
                </div>
            </div>
            <div class="row">
                {% for member in site.ambulances %}
                <div class="col-sm-4">
                    <div class="ambulance-unit">
                        <img src="img/ambulance/{{ member.pic }}.jpg" class="img-responsive img-circle" alt="">
                        <h4>{{ member.make }}</h4>
                        <p class="text-muted">{{ member.position }}</p>
                    </div>
                </div>
                {% endfor %}
            </div>
            <div class="row">
                <div class="col-lg-8 col-lg-offset-2 text-center">
                    <p class="large text-muted">Our ambulance are awesome!!</p>
                </div>
            </div>
        </div>
    </section>
